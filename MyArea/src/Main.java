import static AreaOfCircle.MyArea.area;
public class Main {
    public static void main(String[] args) {
        double radius = 10;
        double area = area(radius);
        System.out.println(area);
    }
}
